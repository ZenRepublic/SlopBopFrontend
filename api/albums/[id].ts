// /albums/:id — see api/_lib/collectionEmbed.ts. An album is a `collections`
// doc like a mixtape or a jam, so all three routes share one handler.
export { default } from '../_lib/collectionEmbed';
